// Globals
import globals from "./../../config/globals";

const styles = {
  input: {
    border: `1px solid ${globals.colors.lightGray}`,
    borderRadius: 4,
    fontSize: 16,
    height: 24,
    outline: "none",
    width: 220
  },
  label: {
    marginBottom: 5
  }
};

export default styles;
