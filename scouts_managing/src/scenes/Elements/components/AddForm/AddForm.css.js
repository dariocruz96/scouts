// Globals
import globals from "./../../../../config/globals";

const styles = {
  addContainer: {
    display: "center",
    fontFamily: globals.font,
    justifyContent: "center",
    margin: "0 auto",
    width: "75%"
  },
  inputField: {
    marginBottom: 12,
    width: "100%"
  }
};

export default styles;
